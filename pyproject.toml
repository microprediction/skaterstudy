[project]
name = "skaterstudy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.1",
    "requests>=2.32.5",
    "skaters>=0.5.0",
]

[tool.uv.sources]
skaters = { path = "../skaters", editable = true }
